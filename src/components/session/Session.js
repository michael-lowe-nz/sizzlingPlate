import React from 'react'

const Session = ({ title, location }) => (
  <div className="session">
    <h1 className="title is-2">{title}</h1>
    <h2 className="subtitle is-4"></h2>
  </div>
)

export default Session
